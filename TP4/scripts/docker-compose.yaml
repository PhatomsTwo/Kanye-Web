version: '3.8'

services:
  server:
    build: ./server
    ports:
      - "5000:5000"
    environment:
      - PYTHONUNBUFFERED=1
    restart: always

  client:
    build: ./client
    depends_on:
      - server 
    environment:
      - PYTHONUNBUFFERED=1